{
  "name": "slab",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "check": "cargo fmt --check --manifest-path src-tauri/Cargo.toml && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
