{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main dashboard window",
  "windows": ["main", "settings"],
  "permissions": ["core:default", "log:default"]
}
